console.log("API Stands for: ");
// Application Programming Interface 

// console.log(window.document == document);

// console.log(window.location === document.location);
// The window.location object can be used to get the current page address (URL) 
// and to redirect the browser to a new page. The Document.location read-only 
// property returns a Location object, which contains information about the URL 
// of the document and provides methods for changing that URL and loading another URL

// function test(){
//     return this;
// }
// console.log(test() == window);

// In this case, the function's this returns the global/window object. ke. test() retums window object. 
// Thus console.log(test()=window); prints true in the console
